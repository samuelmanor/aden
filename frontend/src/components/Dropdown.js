const Dropdown = () => {
  // gen options from given array via .map => `const option`/etc
  // use given state change function to update parent state on click
  return (
    <div>

    </div>
  )
};

export default Dropdown;